# ===========================================
# The Anti-Agency - Environment Configuration
# ===========================================

# ----- Required -----

# Supabase Configuration
# Get these from your Supabase project settings > API
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# ----- Optional -----

# AI Features (Groq - FREE tier available)
# Sign up at https://console.groq.com to get your API key
# Without this, AI suggestions will show demo responses
GROQ_API_KEY=your-groq-api-key

# Email Notifications (Resend - FREE tier available)
# Sign up at https://resend.com to get your API key
# Without this, emails are logged to console in development
RESEND_API_KEY=your-resend-api-key
EMAIL_FROM=notifications@yourdomain.com
EMAIL_FROM_NAME=Your Site Name

# Base URL (for SEO and email links)
# Set to your production domain in production
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# ----- Development Only -----

# Enable debug logging
# DEBUG=true
